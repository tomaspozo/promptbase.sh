/**
 * Current promptbase template/schema version — mirror of
 * supabase/functions/_templates/promptbase/VERSION. Bump both together when the
 * deployed functions or migration change; the UI flags environments installed
 * on an older version so they can be updated.
 */
export const PROMPTBASE_VERSION = "2026.06.10";
